#include <stdio.h>


extern char **paths;

void get_path(char* path_arr[]) {
    // takes param/s
    // set search to the first param, then if not find go next

    printf("Inside path (linking is fun)");
}